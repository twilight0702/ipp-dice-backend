# 服务器测试环境配置
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/ipp_dice?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: YUyue0702
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    com.ippclub.ippdicebackend: info
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"